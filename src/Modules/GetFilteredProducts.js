import { getProduct } from "../api/api.js";

// Функция для получения товаров по заданным параметрам фильтрации
async function GetFilteredProducts(filterParams) {
    try {
       
    } catch (error) {
        console.error("Error while retrieving data:", error);
    }
 
}

export {GetFilteredProducts} ;
